All current images in this folder are already used by the landing page.

If you want to swap any image, either:
1) Replace the file while keeping the same filename, or
2) Edit image paths in app/page.tsx and app/globals.css.
